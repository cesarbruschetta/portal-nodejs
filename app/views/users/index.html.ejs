<div class="hero-unit">
  <h2>Usúarios</h2>
  <%- linkTo('Criar novo usúario', addUserPath, {class: 'btn pull-right'}); %>
</div>

<% if (users) { %>
<% for (var i = 0, ii = users.length; i < ii; i++) { %>
<div class="row list-item">
  <div class="span8">
    <h3><%- linkTo(users[i].givenName, userPath(users[i].id)); %></h3>
  </div>
  <div class="span4">
    <h3><i class="icon-list-alt"></i><%= users[i].email; %></h3>
  </div>
</div>
<% } %>
<% } %>
