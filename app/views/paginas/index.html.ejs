<div class="hero-unit">
  <h2>All Paginas</h2>
  <%- linkTo('Create a new Pagina', addPaginaPath, {class: 'btn pull-right'}); %>
</div>

<div id="paginas-list">
<% if (paginas) { %>
<% for (var i = 0, ii = paginas.length; i < ii; i++) { %>
  <div class="row list-item" id="pagina-<%= paginas[i].id; %>">
    <div class="span8">
      <h3><%- linkTo(paginas[i].title, paginaPath(paginas[i].id)); %></h3>
    </div>
    <div class="span4">
      <h3><i class="icon-list-alt"></i><%= paginas[i].id; %></h3>
    </div>
  </div>
<% } %>
<% } %>
</div>