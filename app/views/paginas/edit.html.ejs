<div class="hero-unit">
  <form id="pagina-form" class="form-horizontal" action="/paginas/<%= params.id %>?_method=PUT" method="POST">
    <fieldset>
      <legend>Editar a Página</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {pagina: pagina}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Savar', {type: 'submit', class: 'btn btn-primary'}) %>
        <%- contentTag('button', 'Excluir', {type: 'submit', formaction: '/paginas/' + params.id + '?_method=DELETE', formmethod: 'POST', class: 'btn btn-danger'}) %>
      </div>
    </fieldset>
  </form>
</div>
